name: CI

on:
  pull_request:
  push:
    branches:
      - main

env:
  LOG_LEVEL: debug

jobs:
  validate-preset:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        file:
          - rust/updateChannelInRustToolchainToml.json
          - rust/updateRustVersionInCargoToml.json
          - default.json

    env:
      RENOVATE_CONFIG_FILE: ${{ matrix.file }}

    steps:
      - name: Checkout
        uses: actions/checkout@cd6a9fd49371476d813e892956e2e920fcc3fb7e

      - name: Testing ${{ matrix.file }}
        run: npx -p renovate renovate-config-validator
